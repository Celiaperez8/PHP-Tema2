"use strict"
//Dado un array de palabras, usa map() para crear un nuevo array que 
//contenga la longitud de cada palabra


let palabras = [hola, ejemplo, mundo];
let longitudes = palabras.map(palabra => palabra.length);
console.log(longitudes); // [4, 7, 5]
