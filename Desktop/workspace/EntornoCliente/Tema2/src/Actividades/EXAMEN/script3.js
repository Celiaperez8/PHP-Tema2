"use strict"

function palabraMasLarga (palabras) {
    let palabraMasLarga = "";
    for (let palabra of palabras) {
        if (palabra.length > palabraLarga.length) {
            palabraLarga = palabra;
            }
            }
            return palabraLarga;
}

console.log(palabraMasLarga (["hola", "mundo", "python", "programacion"])); // "programacion

